node_modules
.next
.next/
next-env.d.ts
out
.env
.env.local
*.log
.DS_Store
data/
dist/
tsconfig.tsbuildinfo
tsconfig.scripts.tsbuildinfo

# Local-only scratch areas (legacy-app survey, ad-hoc probes, etc.)
recon/
scratch/
.superpowers
# Agent/knowledge-graph tooling output — never part of the deploy context.
graphify-out/
.agents/

# Nested git worktree checkouts (sibling dirs accidentally linked inside repo)
luma-*/
